#include <iostream>

#define SIZE 3

class Vector
{
  double x[SIZE];
  
public:
  Vector() { for (int i=0; i<SIZE; i++) x[i]=0; }
  double operator [] (int arg) const { return this->x[arg]; }
  
};

std::ostream& operator << (std::ostream &str, const Vector &V);
