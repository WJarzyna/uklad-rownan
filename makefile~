CC=g++
CFLAGS=-pedantic -Wall -std=c++14 -iquote inc

DEPS=matrix.hh vect.hh

OBJ=main.o matrix.o vect.o

PROGNAME=urown.x

%.o : %.c $(DEPS)
	$(CC) -c -o $@ $< $(CFLAGS)

testz: $(OBJ) 
	$(CC) -o $(PROGNAME) $(OBJ)

clean:
	rm *.o *~

del:
	rm $(PROGNAME)
